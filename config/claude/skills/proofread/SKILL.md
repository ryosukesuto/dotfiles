---
name: proofread
description: AI生成テキストを校正する。「校正」「proofread」「文体チェック」「AIっぽい」「トーン修正」等で起動。
user-invocable: true
argument-hint: "[ファイルパス | slack]（省略時は直前に書いたファイル。`slack` でSlack向け会話文に校正）"
allowed-tools:
  - Read
  - Edit
  - Agent
---

# /proofread - AI生成テキストの校正

直前に書いた、または指定されたファイルのテキストをユーザーのトーンに合わせて校正する。

## 実行手順

### 1. 対象テキストの特定

引数の解釈:
- ファイルパス: そのファイルを対象にする
- `slack`（または `slack` を含む文字列）: 直前に書いた / 提示したSlack草案テキストを対象にし、Slack向けパターンを併用する
- 省略時: 直前にEdit/Writeしたファイル、または直前のメッセージで提示したテキスト

### 2. チェックリストに沿って検出

対象テキストを読み、以下のパターンを検出する。該当箇所を行番号付きでリストアップする。
自分が書いた文章のパターンは自分では見落としやすいため、チェックリストで機械的に洗い出す。

検出パターン:
- 共通: `${CLAUDE_SKILL_DIR}/patterns.md`
- Slack向け（引数 `slack` のときのみ追加適用）: `${CLAUDE_SKILL_DIR}/patterns-slack.md`

Slack向けの場合、共通patternsの一部（ラベル+改行構造、体言止めの羅列、見出し化など）はドキュメント体を前提とした指針なので、Slack patternsの指示が優先される。

### 3. 修正

検出した箇所をEditで修正する。修正が10箇所以上ある場合は、先にリストを見せて確認を取る。
一括修正すると意図しない変更が混ざるリスクがあるため、多い場合は確認を挟む。

### 4. Reader Testing

修正後、Subagentに修正結果を読ませて違和感がないか確認する。Subagentには修正意図を伝えない。

```
Agent(subagent_type="general-purpose"):
  "以下のテキストを読んで、文体の一貫性をチェックしてください。
   作成の背景は伝えません。テキストだけから判断してください。
   - 文体が急に変わる箇所
   - 不自然に丁寧/説明的な箇所
   - 同じことを繰り返している箇所
   50語以内で報告してください。"
```

問題があれば追加修正。なければ完了。

## Gotchas

- コードブロック内のコメントは校正対象外。コード中のコメントはそのプロジェクトのスタイルに従う
- CLAUDE.mdやSKILL.mdはClaude向けの指示文なので、ユーザーのトーンに寄せすぎない。ただしAIっぽいパターンは除去する
- 英語のテキストはスコープ外。日本語部分のみ対象
