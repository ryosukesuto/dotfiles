{
  "strictness": 2,
  "commentTypes": ["logic", "syntax"],
  "triggerOnUpdates": false,
  "triggerOnDrafts": false,
  "statusCheck": true,
  "summarySection": true,
  "issuesTableSection": true,
  "confidenceScoreSection": true,
  "sequenceDiagramSection": false,
  "ignorePatterns": "dist/**\nnode_modules/**\n.terraform/**\n**/*.tfstate\n**/*.tfstate.backup",
  "instructions": "{{PROJECT_INSTRUCTIONS}}",
  "rules": [
    {
      "id": "iam-no-basic-roles",
      "rule": "IAM は最小権限の原則に従い、roles/editor は使用禁止。roles/owner は必要な場合のみ明示的に管理してください。",
      "severity": "high",
      "scope": ["**/*.tf"]
    },
    {
      "id": "iam-no-public-principals",
      "rule": "IAM バインディングに allUsers / allAuthenticatedUsers を指定しないでください。公開が意図的な場合はコメントで根拠を明示してください。",
      "severity": "high",
      "scope": ["**/*.tf"]
    },
    {
      "id": "wif-attribute-condition",
      "rule": "Workload Identity Federation の provider には attribute_condition でリポジトリ・ブランチ制限を必ず設定してください。",
      "severity": "high",
      "scope": ["**/*.tf"]
    },
    {
      "id": "terraform-declarative-state",
      "rule": "State 変更は import / moved / removed ブロックで宣言的に記述してください。terraform state rm などの手動コマンド依存は避けてください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "gcp-api-disable-on-destroy",
      "rule": "google_project_service には disable_on_destroy = true を設定してください。他リソースへの巻き込み破壊を防止します。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "project-prevent-destroy",
      "rule": "プロジェクトや本番データを保持するリソースには lifecycle.prevent_destroy = true を設定してください。",
      "severity": "high",
      "scope": ["**/*.tf"]
    },
    {
      "id": "terraform-naming",
      "rule": "Terraform 識別子は snake_case、クラウドリソース名はハイフン区切りに統一してください。",
      "severity": "low",
      "scope": ["**/*.tf"]
    },
    {
      "id": "iam-use-member-not-binding",
      "rule": "IAM 管理には加算的な google_*_iam_member を使用してください。google_*_iam_binding / google_*_iam_policy は既存権限を上書きするため禁止です。",
      "severity": "high",
      "scope": ["**/*.tf"]
    },
    {
      "id": "datadog-monitor-delete-disable",
      "rule": "Datadog Monitor の削除や無効化（enabled=false 等）は本番監視対象の場合 P1 です。テスト用 Monitor である旨が明示されていない限り、意図と代替監視を確認してください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "pubsub-delivery-settings",
      "rule": "Pub/Sub subscription の ack_deadline / dead_letter_policy / retry_policy 変更はメッセージ配送挙動に影響します。本番 subscription の場合は意図と影響範囲を確認してください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "cdn-cache-rule-change",
      "rule": "Fastly / Cloudflare のキャッシュルール・バックエンド設定変更はユーザー影響が大きいため、本番CDN前提で P1 扱いにしてください。未リリース環境なら影響を明示してください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "security-oncall-settings",
      "rule": "Wiz / PagerDuty 設定（エスカレーションポリシー・escalation_rule・スケジュール）の変更はセキュリティ検知・オンコール体制に影響します。意図を明示してください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    },
    {
      "id": "alert-threshold-relax",
      "rule": "アラート閾値の緩和（より緩い方向への変更）は検知漏れのリスクがあります。本番アラート前提で P1 扱いとし、緩和根拠を確認してください。",
      "severity": "medium",
      "scope": ["**/*.tf"]
    }
  ],
  "disabledRules": []
}
