[core]
	editor = vim
	pager = delta
	autocrlf = input
	quotepath = false
	precomposeunicode = true
	hooksPath = /Users/s32943/gh/github.com/ryosukesuto/dotfiles/git/hooks

[interactive]
	diffFilter = delta --color-only

[delta]
	features = tokyo-night
	navigate = true
	line-numbers = true
	side-by-side = false
	syntax-theme = Catppuccin Mocha

[delta "tokyo-night"]
	minus-style = syntax "#3b2530"
	minus-emph-style = syntax "#552a3e"
	plus-style = syntax "#1f3b36"
	plus-emph-style = syntax "#2b5a4a"
	line-numbers-minus-style = "#f7768e"
	line-numbers-plus-style = "#9ece6a"
	line-numbers-zero-style = "#3b4261"
	line-numbers-left-style = "#3b4261"
	line-numbers-right-style = "#3b4261"
	file-style = "#7aa2f7" bold
	file-decoration-style = "#7aa2f7" ul
	hunk-header-style = "#bb9af7" bold
	hunk-header-decoration-style = "#414868" box
	hunk-header-file-style = "#7dcfff"
	hunk-header-line-number-style = "#e0af68"

[color]
	ui = auto
	diff = auto
	status = auto
	branch = auto

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold

[color "status"]
	added = green
	changed = yellow
	untracked = red

[pull]
	rebase = true

[push]
	default = simple
	autoSetupRemote = true

[fetch]
	prune = true

[rebase]
	autoStash = true

[diff]
	colorMoved = default
	algorithm = histogram

[merge]
	conflictstyle = diff3

[alias]
	st = status
	co = checkout
	br = branch
	ci = commit
	cm = commit -m
	ca = commit --amend
	unstage = reset HEAD --
	last = log -1 HEAD
	lg = log --oneline --graph --decorate
	lga = log --oneline --graph --decorate --all
	show-graph = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	undo = reset --soft HEAD^
	amend = commit --amend --no-edit
	fixup = commit --fixup
	squash = commit --squash
	praise = blame
	visual = !gitk

[ghq]
	root = ~/gh

[wt]
	basedir = ".worktrees"

# worktree環境でのlock競合を軽減
[gc]
	auto = 0

[maintenance]
	auto = false

# ユーザー情報は.gitconfig.localに記載してください
# 例:
# [user]
#     name = Your Name
#     email = your.email@example.com

[include]
	path = ~/.gitconfig.local
