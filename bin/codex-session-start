#!/usr/bin/env bash
# Reset shared session markers when a Codex session starts.

set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)

rm -f "$HOME/.claude/.compact-counter" "$HOME/.claude/.session-titled"

"$SCRIPT_DIR/cmux-rename-workspace"
